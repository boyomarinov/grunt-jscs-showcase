(function () {
    'use strict';

    // jscs:disable maximumLineLength
    function FooBar() {
        var foo = 'this is a string that should exceed the maximum line length, set in jscs rules';
    }
})();